<h1>Restaurant list</h1>

<div class="container">
  <ul>
      <% @restaurants.each do |restaurant| %>
        <li>
          <%= restaurant.name %><%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary"%>
        </li>
      <% end %>
  </ul>
</div>
